# Use the public npm registry for this project (overrides any corporate nexus mirror).
registry=https://registry.npmjs.org/

# Local cache so installs never touch the root-owned ~/.npm cache (EPERM workaround).
cache=./.npm-cache

# Avoid proxy interference for registry downloads.
proxy=null
https-proxy=null
