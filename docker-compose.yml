services:
  sofia-jogo:
    build:
      context: .
      dockerfile: Dockerfile
    image: sofia-jogo:latest
    container_name: sofia-jogo
    ports:
      - "0.0.0.0:8180:80"
    restart: unless-stopped
